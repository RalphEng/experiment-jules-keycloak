# Keycloak OIDC Configuration
# Diese Werte werden zur Build-Zeit in die React-App eingebaut

# Keycloak Authority URL (Realm-Endpunkt)
REACT_APP_KEYCLOAK_AUTHORITY=http://localhost:8080/realms/appx-realm

# Client ID (wie in Keycloak konfiguriert)
REACT_APP_KEYCLOAK_CLIENT_ID=appx-frontend

# Redirect URI nach erfolgreichem Login
REACT_APP_KEYCLOAK_REDIRECT_URI=http://localhost:3000

# Hinweis: Für Docker-Builds werden diese Werte aus docker-compose.yml übernommen
# Für lokale Entwicklung (npm start): Kopiere diese Datei zu .env
